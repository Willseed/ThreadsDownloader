{
  "name": "@threads-downloader/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./strict-json": "./src/strict-json.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "coverage": "vitest run --config vitest.config.ts --coverage",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  }
}
